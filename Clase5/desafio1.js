/* const numeroAleatorio = () => {
    return Math.round(Math.random() * 19) + 1;
}

const arrayNumeros = [];

for (let i=0; i < 10000; i++) {
    let numero = numeroAleatorio();
    arrayNumeros.push(numero);
}

const objeto = [];

for (let i=1; i<=20; i++) {
    let total = arrayNumeros.filter(item => item === i);
    objeto[i] = total.length;
}

console.log(objeto); */

